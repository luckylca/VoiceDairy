# VoiceDairy depends on recently published native packages that may lag on mirrors.
# Keep this project on the official npm registry and do not rewrite explicit
# registry.npmjs.org tarball URLs using a user-level mirror configuration.
registry=https://registry.npmjs.org/
replace-registry-host=never
